# trifid-adminapp

React, Vite, and TypeScript foundation for TriFid internal staff surfaces.

Run `npm install`, copy `.env.example` to `.env`, then use `npm run dev`.

The initial route is intentionally minimal. `src/components`, `src/services`, `src/types`, `src/hooks`, and `src/utils` are extension points for future work.
